#!/usr/bin/env ruby

# NOTE: `require_relative` specifically not used here on the
# off-chance someone is attempting run this with Ruby 1.8.7
require File.expand_path("../lib/resume", File.dirname(__FILE__)))
Resume.generate
